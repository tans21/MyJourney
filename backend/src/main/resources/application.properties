spring.application.name=myjourney

# DB configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/travelDetails
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
# drops and creates the table everytime the application is started
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect


debug=true

# JWT Secret Key
jwt.secretKey=asdfnads9f67as98dfgvaisudhfa98s67dfy89aishudfuavs89gfvyasi8df76a89sdf7y

#swagger
springdoc.swagger-ui.path=/swagger-ui.html

#http://localhost:8080/swagger-ui/index.html#/ link to run swagger

gemini.api.key=GemeniKey